import React from "react";
import { data } from "../restApi.json";
const Team = () => {
  return (
    <section className="team" id="team">
      <div className="container">
        <div className="heading_section">
          <h1 className="heading">OUR TEAM</h1>
          <p>
            At Kushina, our team is a passionate group of culinary experts and
            hospitality professionals dedicated to creating an unforgettable
            dining experience for you. <br /> Led by our finest chefs Sanji, Soma,
            Teuchi, and Itachi, we work together to bring you the finest flavors,
            ensuring every visit is special.
          </p>
        </div>
        <div className="team_container">
          {data[0].team.map((element) => {
            return (
              <div className="card" key={element.id}>
                <img src={element.image} alt={element.name} />
                <h3>{element.name}</h3>
                <p>{element.designation}</p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
};

export default Team;
